module.exports = commander = require('commander');

commander
    .version('1.0.0')
    .option('start', 'start server')
	.option('stop', 'stop server')
	.option('restart', 'restart server')
	.option('status', 'check if the server is running')
	.option('switch', 'switch to screen');