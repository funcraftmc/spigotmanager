module.exports = nconf = require('nconf');

nconf.env().file({
    file: 'config.json'
});

nconf.defaults({
    name: 'Spigot',
    user: 'minecraft',
    folder: '/minecraft',
    file: 'spigot.jar',
    screen: true,
    screen_name: 'minecraft',
    maxheap: 2048,
    minheap: 1024,
    history: 1024,
    cpu_count: 1,
    java_cmd: 'java'
});